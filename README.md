# 刻印快速筛选分解小助手

这是独立于爬塔项目的本地版本仓库。`src/` 是可恢复的程序输入；早期核心模块仅存有 Python 3.11 字节码，升级前请保留这些 `.pyc` 文件。`release/刻印分解独立版.exe` 保存 v2.0 启动程序，运行时还需要发布目录中的 `_internal/` 依赖。

v2.0 功能：手动点开刻印后，按所选模式进行强化、保留或分解；F9 开始，F10 停止。游戏操作与识别逻辑请先在真实窗口验证。

本仓库尚未选择开源许可证；不能仅凭界面署名代码限制他人修改。
